version: "3.8"

services:
  payment-gateway:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./internal/config/config.yaml:/app/internal/config/config.yaml:ro
    restart: unless-stopped
